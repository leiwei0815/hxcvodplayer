# .gitignore 说明

本文件配置了 Git 应该忽略的文件和目录，避免临时文件、构建产物被提交到仓库。

## 已忽略的目录和文件

### 📦 构建目录
```
build/                      # 通用构建目录
build-*/                    # 所有 build- 开头的目录（如 build-xcframework、build-static-macos 等）
cmake-build-*/              # CMake 构建目录
output/                     # 构建输出目录（XCFramework、静态库等）
```

**说明**：
- `build-xcframework/` - XCFramework 构建临时目录
- `build-static-macos/` - macOS 静态库构建目录
- `build-windows/` - Windows 库构建目录
- `output/` - 所有构建脚本的最终输出目录

### 🍎 Apple 平台
```
*.xcodeproj                 # Xcode 项目文件（通过 CMake 生成）
*.xcworkspace               # Xcode 工作空间
DerivedData/                # Xcode 派生数据
apple/build_xcframework/    # Apple 平台构建目录
```

### 🤖 Android 平台
```
*.iml                       # IntelliJ/Android Studio 模块文件
.gradle/                    # Gradle 构建缓存
local.properties            # 本地配置（SDK 路径等）
.externalNativeBuild/       # CMake/NDK 构建缓存
.cxx/                       # CMake 构建输出
```

### 🔧 第三方库编译目录
```
macos-third/ffmpeg-build-macos/
macos-third/soundtouch-build-macos/
ios-third/ffmpeg-build-ios/
ios-third/soundtouch-build-ios/
android-third/ffmpeg-build-android/
android-third/soundtouch-build-android/
android-third/mbedtls-build-android/
```

**说明**：这些是通过脚本编译的第三方库，体积较大，不应提交到仓库。

### 💻 IDE 文件
```
.vscode/                    # VS Code 配置
.idea/                      # IntelliJ IDEA/CLion 配置
*.swp, *.swo, *~            # Vim 临时文件
.DS_Store                   # macOS 文件夹配置
```

### 🎬 测试文件
```
*.mp4, *.mkv, *.avi, *.flv  # 视频文件
test_videos/                # 测试视频目录
```

### 📝 编译产物
```
*.o, *.obj                  # 目标文件
*.a, *.lib                  # 静态库
*.so, *.dylib, *.dll        # 动态库
*.exe                       # 可执行文件
```

### 🛠️ CMake 生成文件
```
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
```

### 🎨 Qt 生成文件
```
*.pro.user
*.pro.user.*
moc_*.cpp                   # Qt 元对象编译器生成
ui_*.h                      # Qt UI 编译器生成
qrc_*.cpp                   # Qt 资源编译器生成
```

---

## 📌 注意事项

### 应该提交的文件
✅ 源代码（`.cpp`, `.h`, `.mm` 等）
✅ CMakeLists.txt
✅ 构建脚本（`scripts/*.sh`, `scripts/*.ps1`）
✅ 配置文件（`.gitignore`, `README.md` 等）
✅ 第三方库构建脚本（如 `build_ffmpeg_*.sh`）

### 不应该提交的文件
❌ 编译产物（`.a`, `.dll`, `.xcframework` 等）
❌ 构建临时目录（`build-*/`, `output/` 等）
❌ IDE 配置文件（`.vscode/`, `.idea/` 等）
❌ 第三方库编译结果（`*-third/*-build-*/`）

---

## 🔄 清理已追踪的文件

如果某些文件已经被 Git 追踪，但现在想忽略，使用：

```bash
# 从 Git 中移除（但保留本地文件）
git rm -r --cached build-xcframework/
git rm -r --cached output/

# 提交更改
git commit -m "从版本控制中移除构建目录"
```

---

## 📚 相关文档

- [构建脚本使用指南](scripts/README.md)
- [跨平台库构建指南](CROSS_PLATFORM_LIBRARY_GUIDE.md)
