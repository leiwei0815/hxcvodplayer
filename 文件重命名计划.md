# 项目文件重命名计划

## 重命名规则
- 所有 include 目录的头文件添加 `yx` 前缀
- 所有 src/core 目录的源文件添加 `yx` 前缀
- 保持小写命名风格（如 `player_core.h` → `yx_player_core.h`）

## include 目录文件重命名映射

| 原文件名 | 新文件名 | 是否重命名 |
|---------|---------|-----------|
| `debug_helper.h` | `yx_debug_helper.h` | ✅ |
| `decoder.h` | `yx_decoder.h` | ✅ |
| `frame_queue.h` | `yx_frame_queue.h` | ✅ |
| `logger.h` | `yx_logger.h` | ✅ |
| `packet_queue.h` | `yx_packet_queue.h` | ✅ |
| `platform_interface.h` | `yx_platform_interface.h` | ✅ (但建议删除) |
| `player_core.h` | `yx_player_core.h` | ✅ |
| `player_core_c_bridge.h` | `yx_player_core_c_bridge.h` | ✅ |
| `player_types.h` | `yx_player_types.h` | ✅ |

## src/core 目录文件重命名映射

| 原文件名 | 新文件名 | 是否重命名 |
|---------|---------|-----------|
| `decoder.cpp` | `yx_decoder.cpp` | ✅ |
| `packet_queue.cpp` | `yx_packet_queue.cpp` | ✅ |
| `player_core.cpp` | `yx_player_core.cpp` | ✅ |
| `player_core_c_bridge.cpp` | `yx_player_core_c_bridge.cpp` | ✅ |
| `player_types.cpp` | `yx_player_types.cpp` | ✅ |
| `CMakeLists.txt` | `CMakeLists.txt` | ❌ 保持不变 |

## 需要更新引用的文件

### 1. Core 源文件
- `src/core/*.cpp` - 更新 #include 语句
- `src/core/CMakeLists.txt` - 更新源文件列表

### 2. Desktop 源文件
- `src/desktop/main_window.cpp`
- `src/desktop/main_window.h`
- `src/desktop/main.cpp`
- `src/desktop/video_widget.cpp`
- `src/desktop/video_widget.h`
- `src/desktop/CMakeLists.txt`

### 3. iOS 源文件
- `src/ios/YXPlayerControl.mm`
- `src/ios/build/CMakeLists.txt`

### 4. 根目录
- `CMakeLists.txt`

## 执行步骤

1. ✅ 重命名 include 目录文件
2. ⏳ 重命名 src/core 目录文件  
3. ⏳ 更新所有 #include 引用
4. ⏳ 更新所有 CMakeLists.txt
5. ⏳ 编译测试（Desktop + iOS）

## 注意事项

- ⚠️ 建议先删除未使用的 `platform_interface.h` 相关文件
- ⚠️ 大量文件重命名，需要仔细验证
- ⚠️ Git 用户应使用 `git mv` 保留历史记录
